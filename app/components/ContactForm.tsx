"use client";
import { FormEvent, useState } from "react";
import { contact } from "../data";
export function ContactForm(){const [status,setStatus]=useState<"idle"|"sending"|"sent"|"fallback">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("sending");const fd=new FormData(e.currentTarget);const payload=Object.fromEntries(fd);try{const r=await fetch("/api/enquiries",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...payload,type:"contact",consent:fd.get("consent")==="on"})});setStatus(r.ok?"sent":"fallback")}catch{setStatus("fallback")}}if(status==="sent")return <div className="form-status success"><h2>Thank you. Your message was sent.</h2><p>The care team will use the contact details you provided to reply.</p></div>;return <form className="contact-form" onSubmit={submit}><span className="eyebrow">Send an enquiry</span><h2>How can we help?</h2><p>Please keep medical details for your private consultation.</p><div className="field-grid"><label><span>Full name *</span><input name="name" required autoComplete="name"/></label><label><span>Phone *</span><input name="phone" required type="tel" autoComplete="tel"/></label><label className="wide"><span>Email</span><input name="email" type="email" autoComplete="email"/></label><label className="wide"><span>Message</span><textarea name="message" rows={5} maxLength={1500}/></label><input className="honeypot" name="website" tabIndex={-1} autoComplete="off"/><label className="consent-row wide"><input name="consent" type="checkbox" required/><span>I agree that Kindred Path may use these details to respond to my enquiry.</span></label></div><button className="button button-primary" disabled={status==="sending"}>{status==="sending"?"Sending…":"Send securely"}</button>{status==="fallback"&&<div className="form-status"><p>Email delivery is being configured. Please use WhatsApp or call for now.</p><a href={`https://wa.me/${contact.whatsapp}`} target="_blank" rel="noreferrer">Open WhatsApp →</a></div>}</form>}
