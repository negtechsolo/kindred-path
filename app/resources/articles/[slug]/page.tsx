import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { articles } from "../../../articles";
export const generateStaticParams=()=>articles.map(({slug})=>({slug}));
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const a=articles.find(x=>x.slug===slug);return a?{title:a.title,description:a.description,keywords:a.keywords}:{}}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)notFound();const schema={"@context":"https://schema.org","@type":"MedicalWebPage",headline:a.title,description:a.description,dateModified:"2026-07-22",publisher:{"@type":"Organization",name:"Kindred Path Fertility Centre"},about:a.keywords};return <main id="main-content"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="article-hero"><div className="shell article-hero-inner"><nav className="breadcrumb"><Link href="/resources/articles">Articles</Link><span>›</span><span>{a.category}</span></nav><span className="eyebrow">{a.category} · {a.read}</span><h1>{a.title}</h1><p>{a.excerpt}</p></div></section><section className="section-pad"><div className="shell article-layout"><article className="article-body">{a.sections.map(s=><section key={s.heading}><h2>{s.heading}</h2>{s.paragraphs.map(p=><p key={p}>{p}</p>)}{s.bullets&&<ul>{s.bullets.map(b=><li key={b}>{b}</li>)}</ul>}</section>)}<section><h2>Common questions</h2>{a.faq.map(f=><div className="inline-faq" key={f.question}><h3>{f.question}</h3><p>{f.answer}</p></div>)}</section><section className="sources"><h2>Sources and further reading</h2><ul>{a.sources.map(s=><li key={s.url}><a href={s.url} target="_blank" rel="noreferrer">{s.label}</a></li>)}</ul></section><p className="medical-note">Last reviewed July 2026. This article is educational and does not diagnose or replace individual medical advice.</p></article><aside className="article-aside"><span className="eyebrow">Personalised care</span><h2>Information is a beginning.</h2><p>A specialist can interpret testing, options and evidence for your situation.</p><Link className="button button-primary" href="/book">Request a consultation</Link><Link href="/resources/articles">Browse all articles →</Link></aside></div></section><section className="section-pad soft-section"><div className="shell"><div className="section-heading"><span className="eyebrow">Keep reading</span><h2>Other fertility articles.</h2></div><div className="related-grid">{articles.filter((a)=>a.slug!==slug).slice(0,4).map((a)=><Link key={a.slug} className="related-card" href={`/resources/articles/${a.slug}`}><strong>{a.title}</strong><span>{a.excerpt}</span><em>Read the article →</em></Link>)}</div><p className="related-more"><Link href="/resources/articles">View all articles →</Link></p></div></section></main>}
