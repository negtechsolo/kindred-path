import type { Metadata } from "next";
import Link from "next/link";
import { articles } from "../../articles";
export const metadata: Metadata = { title: "Fertility Articles & Guides", description: "Evidence-informed fertility articles for people in Lagos and Nigeria, written to help you prepare better questions for specialist care." };
export default function ArticlesPage(){return <main id="main-content"><section className="page-hero resource-hero"><div className="shell narrow-page-hero"><span className="eyebrow light">Kindred Path knowledge centre</span><h1>Useful fertility answers, <em>without the noise.</em></h1><p>Twelve practical, evidence-informed articles designed to create real value, not keyword-stuffed promises.</p></div></section><section className="section-pad"><div className="shell"><div className="article-grid">{articles.map((article)=><article className="article-card" key={article.slug}><span className="treatment-category">{article.category}</span><h2>{article.title}</h2><p>{article.excerpt}</p><div><small>{article.read}</small><Link href={`/resources/articles/${article.slug}`}>Read article →</Link></div></article>)}</div></div></section></main>}
